# create vocab and counters for subset
vocab = set()
counters = list()

for prime, target in subset:
    for w in prime.split():
        vocab.add(w)

    for w in target.split():
        vocab.add(w)

    prime_counter = Counter(prime)
    target_counter = Counter(target)
    
    counter.append(prime_counter, target_counter)
    

c_count = {w: list() for w in vocab}
y = {w: list() for w in vocab}


for w in vocab:
    for prime, target in counters:
        c_count[w].append(prime[w])
        y[w].append(target[w])
